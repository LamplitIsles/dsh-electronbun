// The startup view is mostly inline HTML so Hutch can package it without a
// second frontend toolchain. This entrypoint is retained for Electrobun's
// view graph and can be extended by a product adapter.
export {};
